# dashboard-airdrop
web dashboard for airdrop crypto


1. Install PHP
   ```bash
   sudo apt update
   sudo apt install php -y
   ```
   ```bash
   php -v
   ```
   ```bash
   cd /var/www/html
   ```

   

1. Install PHP
